import { withInstall } from "@element-plus-ui/pro-utils";
import Checkbox from "./Checkbox";
import CheckboxButton from "./CheckboxButton";

export * from "./typing";

export const ProFieldCheckbox = withInstall(Checkbox);

export const ProFieldCheckboxButton = withInstall(CheckboxButton);
